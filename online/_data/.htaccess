# Block direct HTTP access to counter storage (Apache 2.4+)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
